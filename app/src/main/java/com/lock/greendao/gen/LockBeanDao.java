package com.lock.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.oulu.lock.beans.LockBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LOCK_BEAN".
*/
public class LockBeanDao extends AbstractDao<LockBean, Long> {

    public static final String TABLENAME = "LOCK_BEAN";

    /**
     * Properties of entity LockBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property LockName = new Property(1, String.class, "lockName", false, "LOCK_NAME");
        public final static Property Address = new Property(2, String.class, "address", false, "ADDRESS");
        public final static Property CustomName = new Property(3, String.class, "customName", false, "CUSTOM_NAME");
        public final static Property Isable = new Property(4, int.class, "isable", false, "ISABLE");
    }


    public LockBeanDao(DaoConfig config) {
        super(config);
    }
    
    public LockBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LOCK_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"LOCK_NAME\" TEXT," + // 1: lockName
                "\"ADDRESS\" TEXT UNIQUE ," + // 2: address
                "\"CUSTOM_NAME\" TEXT," + // 3: customName
                "\"ISABLE\" INTEGER NOT NULL );"); // 4: isable
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LOCK_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LockBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String lockName = entity.getLockName();
        if (lockName != null) {
            stmt.bindString(2, lockName);
        }
 
        String address = entity.getAddress();
        if (address != null) {
            stmt.bindString(3, address);
        }
 
        String customName = entity.getCustomName();
        if (customName != null) {
            stmt.bindString(4, customName);
        }
        stmt.bindLong(5, entity.getIsable());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LockBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String lockName = entity.getLockName();
        if (lockName != null) {
            stmt.bindString(2, lockName);
        }
 
        String address = entity.getAddress();
        if (address != null) {
            stmt.bindString(3, address);
        }
 
        String customName = entity.getCustomName();
        if (customName != null) {
            stmt.bindString(4, customName);
        }
        stmt.bindLong(5, entity.getIsable());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public LockBean readEntity(Cursor cursor, int offset) {
        LockBean entity = new LockBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // lockName
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // address
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // customName
            cursor.getInt(offset + 4) // isable
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LockBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setLockName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setAddress(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setCustomName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setIsable(cursor.getInt(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(LockBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(LockBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(LockBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
