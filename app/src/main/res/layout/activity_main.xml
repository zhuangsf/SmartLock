<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/main_title_bar"
    android:fitsSystemWindows = "true"
    android:orientation="vertical" >

    <!--  <android.support.v4.view.ViewPager
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:id="@+id/id_view_pager"/>
  -->

    <FrameLayout
        android:id="@+id/fragmentfield"
        android:layout_width="fill_parent"
        android:fitsSystemWindows = "true"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <RadioGroup
        android:id="@+id/tab_menu"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="#FFFFFF"
        android:orientation="horizontal" >
        <!--   android:background="@drawable/mmfooter_bg"  -->

        <RadioButton
            android:id="@+id/rbLockList"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/tab_selector_time"
            android:gravity="center_horizontal|bottom"
            android:paddingTop="12dp"
            android:checked="true"/>

        <RadioButton
            android:id="@+id/rbLockManger"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/tab_selector_data"
            android:gravity="center_horizontal|bottom"
            android:paddingTop="12dp"

            />

        <RadioButton
            android:id="@+id/rbPersonal"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/tab_selector_home"
            android:paddingTop="12dp"
            />

    </RadioGroup>



</LinearLayout>